---
description: Admin Portal Design System - Typography, components, and patterns for internal tooling
globs: *.tsx,*.jsx,*.css
alwaysApply: false
---

# Admin Portal Design System

**Status**: Baseline established from DBP-325 Figma designs
**Last Updated**: 2026-01-13
**Figma Reference**: [Admin Portal Designs](https://www.figma.com/design/wnIZMZLYKc8LT7MpANvPVy/App-Designs?node-id=3535-30118)

---

## 1. Design Philosophy

The Admin Portal design system is **distinct from the public-facing bpp-web design system**. It is optimized for:

- **Internal tooling**: Government administrators and data curators
- **Data density**: Tables, lists, and information-rich interfaces
- **Desktop-first**: Not designed for mobile/responsive (per requirements)
- **Efficiency**: Rapid task completion over marketing aesthetics

**Key Difference**: Admin uses smaller typography, tighter spacing, and sidebar-based navigation vs. web's header-based navigation.

---

## 2. Typography System

### 2.1 Font Configuration

**Primary Font**: Roboto via `next/font/google`

**Weights Required**:

- 400 (Regular)
- 500 (Medium) - **NEW for admin**
- 600 (SemiBold) - **NEW for admin**
- 700 (Bold)
- 800 (ExtraBold) - if needed for future headers

**Configuration** (`app/layout.tsx`):

```tsx
import { Roboto } from 'next/font/google'

const roboto = Roboto({
  weight: ['400', '500', '600', '700', '800'],
  subsets: ['latin'],
  variable: '--font-roboto',
  display: 'swap',
})
```

### 2.2 Typography Scale (Admin-Specific)

| Element                    | Size | Line Height | Weight         | Usage                       | Token              |
| -------------------------- | ---- | ----------- | -------------- | --------------------------- | ------------------ |
| **Page Title**             | 30px | 36px        | 700 (Bold)     | H1 page headings            | `heading-md`       |
| **Section Heading**        | 20px | Normal      | 700 (Bold)     | Section headers (if needed) | Custom             |
| **Sidebar Menu (Active)**  | 14px | 20px        | 500 (Medium)   | Active navigation item      | `text-sm/medium`   |
| **Sidebar Menu (Default)** | 14px | 20px        | 400 (Regular)  | Default navigation item     | `text-sm/normal`   |
| **Sidebar Header**         | 14px | None (1)    | 600 (SemiBold) | "[Name] Admin Portal"       | `text-sm/semibold` |
| **Sidebar Subtext**        | 12px | None (1)    | 400 (Regular)  | "Super Admin View", email   | `text-xs/normal`   |
| **Table Header**           | 14px | 20px        | 500 (Medium)   | Column headers              | `text-sm/medium`   |
| **Table Body**             | 14px | 20px        | 400 (Regular)  | Table cell content          | `text-sm/normal`   |
| **Button Text**            | 14px | 20px        | 500 (Medium)   | All button labels           | `text-sm/medium`   |
| **Input Text**             | 14px | 20px        | 400 (Regular)  | Form inputs, placeholders   | `text-sm/normal`   |

### 2.3 Typography Implementation

**Design Token Variables** (from Figma):

```css
/* Typography tokens */
--font-sans: 'Roboto';
--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;
--font-weight-extrabold: 800;

--text-xs-font-size: 12px;
--text-sm-font-size: 14px;
--text-sm-line-height: 20px;

--heading-md-font-size: 30px;
--heading-md-line-height: 36px;
--heading-md-font-weight: 700;
```

**Tailwind Classes**:

```tsx
// Page title (H1)
<h1 className="text-[30px] leading-[36px] font-bold">Targeted Resources</h1>

// Sidebar menu item (active)
<p className="text-sm leading-5 font-medium">Dashboard</p>

// Sidebar menu item (default)
<p className="text-sm leading-5 font-normal">Workspace</p>

// Sidebar header text
<p className="text-sm font-semibold">[Name] Admin Portal</p>

// Sidebar subtext
<p className="text-xs font-normal">Super Admin View</p>

// Table header
<p className="text-sm leading-5 font-medium text-muted-foreground">Resource</p>

// Table body
<p className="text-sm leading-5 font-normal text-foreground">Business Information Center</p>
```

### 2.4 Semantic HTML with Admin Styling

**Page Titles**: Use `<h1>` for accessibility, styled to 30px:

```tsx
// ✅ Correct - semantic HTML with custom admin styling
<h1 className="text-[30px] leading-[36px] font-bold text-foreground">{pageTitle}</h1>
```

**Rationale**: Admin page titles are semantic headings but styled for dense UI, not marketing scale.

---

## 3. Color System

### 3.1 OKLCH Color Palette

**Admin-Specific Colors** (converted from Figma hex):

```css
/* Sidebar colors */
--sidebar-background: oklch(0.98 0 0); /* #fafafa */
--sidebar-foreground: oklch(0.15 0 0); /* #0a0a0a */
--sidebar-primary: oklch(0.2 0 0); /* #171717 */
--sidebar-accent: oklch(0.96 0 0); /* #f5f5f5 - active state */
--sidebar-accent-foreground: oklch(0.2 0 0); /* #171717 */

/* Shared with bpp-web */
--background: oklch(1 0 0); /* #ffffff */
--foreground: oklch(0.145 0 0); /* #0f172a */
--border: oklch(0.922 0 0); /* #e5e5e5 */
--input: oklch(0.922 0 0); /* #e5e5e5 */
--muted-foreground: oklch(0.556 0 0); /* #737373 */
--secondary: oklch(0.96 0 0); /* #f5f5f5 */
--secondary-foreground: oklch(0.2 0 0); /* #171717 */
```

### 3.2 Color Usage Patterns

| Element                 | Color Token          | Hex     | Usage                        |
| ----------------------- | -------------------- | ------- | ---------------------------- |
| Sidebar background      | `sidebar-background` | #fafafa | Fixed sidebar                |
| Sidebar text (default)  | `sidebar-foreground` | #0a0a0a | Menu items, user info        |
| Sidebar icon background | `sidebar-primary`    | #171717 | Avatar/icon containers       |
| Sidebar active state    | `sidebar-accent`     | #f5f5f5 | Active menu item background  |
| Page background         | `background`         | #ffffff | Main content area            |
| Page text               | `foreground`         | #0f172a | Headers, body text           |
| Table headers           | `muted-foreground`   | #737373 | Column headers               |
| Borders                 | `border`             | #e5e5e5 | Table borders, input borders |
| Secondary buttons       | `secondary`          | #f5f5f5 | "Add Resource" button        |

### 3.3 Color Application

```tsx
// Sidebar
<div className="bg-sidebar-background text-sidebar-foreground">
  {/* Sidebar content */}
</div>

// Active menu item
<div className="bg-sidebar-accent text-sidebar-accent-foreground">
  <p className="font-medium">Targeted Resources</p>
</div>

// Table header
<th className="text-muted-foreground font-medium">Resource</th>

// Table cell
<td className="text-foreground">Business Information Center</td>
```

---

## 4. Layout & Spacing

### 4.1 Spacing Scale (Design Tokens)

```css
--spacing-0-5: 2px;
--spacing-1: 4px;
--spacing-2: 8px;
--spacing-2-5: 10px;
--spacing-3: 12px;
--spacing-4: 16px;
--spacing-6: 24px;
```

**Tailwind Equivalents**:

- `gap-0.5` = 2px
- `gap-1` = 4px
- `gap-2` = 8px
- `gap-3` = 12px
- `gap-4` = 16px
- `gap-6` = 24px

### 4.2 Layout Structure

**Sidebar**:

- Width: `256px` (fixed)
- Padding: `8px` (spacing/2)
- Menu item height: `32px`
- Menu item padding: `8px` (spacing/2)
- Gap between menu items: `4px` (spacing/1)

**Header**:

- Height: `64px` (h-16)
- Horizontal padding: `24px` (spacing/6)
- Border bottom: `1px solid border`

**Page Header** (below main header):

- Padding: `24px` (spacing/6)
- Border bottom: `1px solid border`
- Max width: `1280px`

**Content Area**:

- Left margin: `256px` (sidebar width)
- Max width: `1280px` (constrained content)
- Padding: `16px` (spacing/4) for search/filters

### 4.3 Component Spacing

**Table**:

- Header height: `40px`
- Row height: `57px`
- Cell padding: `8px` (spacing/2)
- Border: `1px solid border`

**Search Input**:

- Height: `36px` (h-9)
- Padding: `12px` horizontal (spacing/3)
- Padding: `4px` vertical (spacing/1)

**Buttons**:

- Height: `36px` (h-9)
- Padding horizontal: `16px` (spacing/4)
- Padding vertical: `8px` (spacing/2)
- Gap between icon and text: `8px` (spacing/2)

---

## 5. Component Patterns

### 5.1 Sidebar Navigation

**Structure**:

```tsx
<aside className="w-64 h-screen bg-sidebar-background flex flex-col">
  {/* Header */}
  <div className="p-2">
    <div className="flex gap-2 items-center p-2 rounded-md">
      <div className="size-8 bg-sidebar-primary rounded-lg flex items-center justify-center">
        <Icon className="size-4 text-sidebar-primary-foreground" />
      </div>
      <div className="flex flex-col gap-0.5">
        <p className="text-sm font-semibold">[Name] Admin Portal</p>
        <p className="text-xs">Super Admin View</p>
      </div>
    </div>
  </div>

  {/* Menu items */}
  <nav className="flex-1 p-2">
    <ul className="flex flex-col gap-1">
      <li>
        <button className="w-full flex gap-2 items-center h-8 px-2 rounded-md hover:bg-sidebar-accent">
          <Icon className="size-4" />
          <span className="text-sm font-normal">Dashboard</span>
          <ChevronRight className="size-4 ml-auto" aria-hidden="true" />
        </button>
      </li>
      {/* Active state */}
      <li>
        <button className="w-full flex gap-2 items-center h-8 px-2 rounded-md bg-sidebar-accent">
          <Icon className="size-4" />
          <span className="text-sm font-medium">Targeted Resources</span>
          <ChevronRight className="size-4 ml-auto" aria-hidden="true" />
        </button>
      </li>
    </ul>
  </nav>

  {/* Footer (user profile) */}
  <div className="p-2">
    <div className="flex gap-2 items-center p-2 rounded-md">
      <div className="size-8 bg-sidebar-primary rounded-lg flex items-center justify-center">
        <CircleUser className="size-4 text-sidebar-primary-foreground" />
      </div>
      <div className="flex flex-col gap-0.5 flex-1">
        <p className="text-sm font-semibold">Darcy Quick</p>
        <p className="text-xs">darcy.quick@example.com</p>
      </div>
      <ChevronsUpDown className="size-4" aria-hidden="true" />
    </div>
  </div>
</aside>
```

### 5.2 Page Header

**Structure**:

```tsx
{
  /* Main header (with breadcrumbs) */
}
;<header className="h-16 border-b border-border flex items-center">
  <div className="max-w-[1280px] px-6 flex gap-2 items-center flex-1">
    <button className="size-7 flex items-center justify-center rounded-md">
      <PanelLeft className="size-4" />
    </button>
    <Separator orientation="vertical" className="h-4" />
    <Breadcrumb>
      <BreadcrumbItem>
        <BreadcrumbLink className="text-sm">Targeted Resources</BreadcrumbLink>
      </BreadcrumbItem>
    </Breadcrumb>
  </div>
</header>

{
  /* Page header (title + actions) */
}
;<div className="border-b border-border p-6">
  <div className="max-w-[1280px] flex items-center justify-between">
    <h1 className="text-[30px] leading-[36px] font-bold">Targeted Resources</h1>
    {/* Actions go here */}
  </div>
</div>
```

### 5.3 Table Pattern

**Structure**:

```tsx
<div className="border border-border rounded-lg overflow-hidden">
  <table className="w-full">
    <thead>
      <tr className="border-b border-border">
        <th className="h-10 px-2 text-left">
          <span className="text-sm font-medium text-muted-foreground">Resource</span>
        </th>
        <th className="h-10 px-2 text-left">
          <span className="text-sm font-medium text-muted-foreground">Acronym</span>
        </th>
      </tr>
    </thead>
    <tbody>
      <tr className="border-b border-border h-[57px]">
        <td className="px-2">
          <a href="#" className="text-sm underline">
            Business Information Center
          </a>
        </td>
        <td className="px-2">
          <span className="text-sm">BIC</span>
        </td>
      </tr>
    </tbody>
  </table>
</div>
```

### 5.4 Search & Filter Bar

**Structure**:

```tsx
<div className="flex items-center justify-between pb-4">
  {/* Search input */}
  <div className="w-[373px]">
    <Input
      type="search"
      placeholder="Search resources"
      className="h-9 text-sm"
      icon={<Search className="size-4" />}
    />
  </div>

  {/* Actions */}
  <Button variant="secondary" className="h-9 gap-2">
    <Plus className="size-4" />
    <span className="text-sm font-medium">Add Resource</span>
  </Button>
</div>
```

### 5.5 Pagination

**Structure**:

```tsx
<div className="flex items-center justify-end gap-1 px-4">
  <Button variant="ghost" size="sm" className="h-9 gap-1">
    <ChevronLeft className="size-4" />
    <span className="text-sm font-medium">Previous</span>
  </Button>

  <Button variant="ghost" size="icon" className="size-9">
    <span className="text-sm font-medium">1</span>
  </Button>

  <Button variant="outline" size="icon" className="size-9">
    <span className="text-sm font-medium">2</span>
  </Button>

  <Button variant="ghost" size="icon" className="size-9">
    <span className="text-sm font-medium">3</span>
  </Button>

  <Button variant="ghost" className="size-9 p-2.5">
    <Ellipsis className="size-4" />
  </Button>

  <Button variant="ghost" size="sm" className="h-9 gap-1">
    <span className="text-sm font-medium">Next</span>
    <ChevronRight className="size-4" />
  </Button>
</div>
```

---

## 6. Border Radius & Shadows

### 6.1 Border Radius Tokens

```css
--border-radius-md: 8px; /* Buttons, inputs, menu items */
--border-radius-lg: 10px; /* Tables, icon containers */
```

**Usage**:

- Sidebar menu items: `rounded-md` (8px)
- Buttons: `rounded-md` (8px)
- Inputs: `rounded-md` (8px)
- Tables: `rounded-lg` (10px)
- Icon containers: `rounded-lg` (10px)

### 6.2 Shadow Tokens

```css
/* Shadow XS (for inputs, buttons) */
--shadow-xs: 0px 1px 2px 0px rgba(0, 0, 0, 0.05);
```

**Usage**:

- Search input: `shadow-xs`
- Secondary buttons: `shadow-xs`
- Pagination active state: `shadow-xs`

---

## 7. Icons

### 7.1 Icon Library

**Source**: Lucide React (already installed via shadcn/ui)

**Icon Size**: `16x16px` (size-4) for all admin icons

### 7.2 Admin Portal Icons

| Section              | Icon Component                            | Purpose                                                   |
| -------------------- | ----------------------------------------- | --------------------------------------------------------- |
| Dashboard            | `LayoutDashboard`                         | Dashboard nav item                                        |
| URL Sources          | `Link`                                    | URL sources nav item (NOTE: Story calls this "Ingestion") |
| Targeted Resources   | `Library`                                 | Targeted resources nav item                               |
| Ingestion            | `RefreshCw`                               | Ingestion nav item                                        |
| Audit Log            | `ListTodo`                                | Audit log nav item                                        |
| Sidebar toggle       | `PanelLeft`                               | Collapse/expand sidebar                                   |
| User avatar          | `CircleUser`                              | User profile icon                                         |
| Dropdown             | `ChevronsUpDown`                          | User menu dropdown                                        |
| Navigation indicator | `ChevronRight`                            | Menu item chevron (decorative)                            |
| Search               | `Search`                                  | Search input icon                                         |
| Add                  | `Plus`                                    | Add resource button                                       |
| Pagination           | `ChevronLeft`, `ChevronRight`, `Ellipsis` | Pagination controls                                       |

### 7.3 Icon Usage

```tsx
import { LayoutDashboard, Library, ChevronRight } from "lucide-react"

// In sidebar menu
<LayoutDashboard className="size-4" />
<span className="text-sm">Dashboard</span>
<ChevronRight className="size-4 ml-auto" aria-hidden="true" />
```

---

## 8. Component Library (shadcn/ui)

### 8.1 Required Components

Install the following shadcn/ui components for admin portal:

```bash
npx shadcn@latest add button
npx shadcn@latest add input
npx shadcn@latest add table
npx shadcn@latest add separator
npx shadcn@latest add breadcrumb
```

### 8.2 Component Customization

**Button Variants** (already in shadcn):

- `default` - Primary actions (not heavily used in admin)
- `secondary` - "Add Resource" button (bg-secondary)
- `ghost` - Pagination buttons, icon buttons
- `outline` - Pagination active state

**Input Customization**:

- Height: `h-9` (36px)
- Font size: `text-sm` (14px)
- Icon support: Search icon inside input

**Table Customization**:

- Header height: `h-10` (40px)
- Row height: `h-[57px]` (custom)
- Cell padding: `px-2` (8px)

---

## 9. Responsive & Accessibility

### 9.1 Responsive Strategy

**Desktop-First**: Admin portal is NOT responsive per requirements.

**Target Resolution**: 1440px+ desktop displays

**No Mobile Support**: Sidebar, tables, and layouts are fixed-width.

### 9.2 Accessibility Requirements

**Keyboard Navigation**:

- All menu items must be keyboard accessible
- Tab through sidebar → header → table → pagination
- Enter/Space to activate buttons and links

**Screen Reader Support**:

- Semantic HTML: `<nav>`, `<aside>`, `<header>`, `<main>`, `<table>`
- ARIA labels for icon-only buttons
- `aria-hidden="true"` for decorative chevrons
- Table headers properly associated with cells

**Focus States**:

- Visible focus indicators (2px outline, 2px offset)
- See `accessibility.mdc` for full requirements

---

## 10. Differences from bpp-web Design System

| Aspect            | bpp-web (Public)            | bpp-admin (Internal)        |
| ----------------- | --------------------------- | --------------------------- |
| **Typography**    | 48px H1, 16px body          | 30px page title, 14px body  |
| **Navigation**    | Header-based                | Sidebar-based               |
| **Layout**        | Content-first, spacious     | Data-dense, efficient       |
| **Responsive**    | Mobile-first (360px+)       | Desktop-only (1440px+)      |
| **Color Palette** | Marketing-focused           | Neutral grays, data-focused |
| **Spacing**       | Generous (24px, 32px gaps)  | Compact (8px, 4px gaps)     |
| **Font Weights**  | 400, 700, 800               | 400, 500, 600, 700          |
| **Components**    | Hero, cards, marketing CTAs | Tables, forms, data views   |

---

## 11. Design Token Reference

### 11.1 Complete Token List (from Figma)

```css
/* Sidebar */
--sidebar-background: #fafafa;
--sidebar-foreground: #0a0a0a;
--sidebar-primary: #171717;
--sidebar-primary-foreground: #fafafa;
--sidebar-accent: #f5f5f5;
--sidebar-accent-foreground: #171717;

/* Base colors */
--background: #ffffff;
--foreground: #0f172a;
--border: #e5e5e5;
--input: #e5e5e5;
--muted-foreground: #737373;
--secondary: #f5f5f5;
--secondary-foreground: #171717;

/* Typography */
--font-sans: 'Roboto';
--font-weight-normal: 400;
--font-weight-medium: 500;
--font-weight-semibold: 600;
--font-weight-bold: 700;
--text-xs-font-size: 12px;
--text-sm-font-size: 14px;
--text-sm-line-height: 20px;
--heading-md-font-size: 30px;
--heading-md-line-height: 36px;
--heading-md-font-weight: 700;

/* Spacing */
--spacing-0-5: 2px;
--spacing-1: 4px;
--spacing-2: 8px;
--spacing-2-5: 10px;
--spacing-3: 12px;
--spacing-4: 16px;
--spacing-6: 24px;

/* Sizing */
--width-w-4: 16px;
--width-w-7: 28px;
--width-w-8: 32px;
--width-w-9: 36px;
--height-h-4: 16px;
--height-h-7: 28px;
--height-h-8: 32px;
--height-h-9: 36px;
--height-h-16: 64px;

/* Border */
--border-width-border: 1px;
--border-radius-md: 8px;
--border-radius-lg: 10px;

/* Shadows */
--shadow-xs: 0px 1px 2px 0px rgba(0, 0, 0, 0.05);

/* Breakpoint */
--breakpoint-2xl: 1440px;
```

---

## 12. Implementation Checklist

When implementing admin portal features:

- [ ] Use 14px (text-sm) as default text size, not 16px
- [ ] Page titles are 30px (heading-md), not 48px
- [ ] Use sidebar color tokens for navigation
- [ ] Tables use 40px header height, 57px row height
- [ ] All icons are 16x16px (size-4)
- [ ] No mobile/responsive breakpoints (desktop-only)
- [ ] Semantic HTML (`<h1>`, `<nav>`, `<table>`) with admin styling
- [ ] ChevronRight icons are decorative (`aria-hidden="true"`)
- [ ] Font weights: 500 (Medium) and 600 (SemiBold) are available
- [ ] Verify all design tokens are defined in `app/globals.css`

---

## 13. Resources

- **Figma**: [Admin Portal Designs](https://www.figma.com/design/wnIZMZLYKc8LT7MpANvPVy/App-Designs?node-id=3535-30118)
- **Story**: `specifications/dbp-325-universal-elements/story.md`
- **shadcn/ui**: [Component Library](https://ui.shadcn.com)
- **Lucide Icons**: [Icon Library](https://lucide.dev)
- **OKLCH Converter**: [oklch.com](https://oklch.com)

---

## 14. Future Considerations

**As the admin portal grows**, document:

1. **New component patterns** (modals, filters, bulk actions)
2. **Data visualization** (charts, graphs for dashboards)
3. **Form patterns** (multi-step wizards, validation states)
4. **Empty states** (no data, no results messaging)
5. **Loading states** (skeletons, spinners for tables)
6. **Error states** (validation errors, API failures)

**Update this document** when new pages are designed in Figma with additional patterns.
